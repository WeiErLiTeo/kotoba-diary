{
  "name": "kotoba-diary",
  "version": "1.0.0",
  "description": "言の葉の記録 - Kotoba Diary powered by Cloudflare Workers",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "preview": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^latest"
  }
}
